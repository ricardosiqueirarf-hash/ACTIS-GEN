# Tarefa: Fase 1 — Fundação Empresarial ACTIS

ID: DEV-2026-09-18-004
Status: IN_PROGRESS
Agente: opencode
Iniciado em: 2026-09-18
Última atualização: 2026-09-18T05:13:18-03:00

## Objetivo

Implementar a fundação multiempresa do ACTIS GEN com organizations, business units, departments, workspaces, users, memberships, roles, permissions, agents, connectors, secrets e audit events, preservando os dados e fluxos existentes.

## Escopo

- Auditar storage, APIs, frontend, runtime e dados atuais.
- Criar modelo organizacional e migration compatível para a organização default.
- Implementar tenant context, RBAC, auditoria e secrets com isolamento backend.
- Tornar agents, connectors, workflows e recursos operacionais tenant-aware.
- Adicionar console mínimo e health check.
- Criar testes de tenant isolation, RBAC, audit, secrets e migration.

## Fora do escopo

- SAML/Entra, billing, Kubernetes, distributed workers, workflow visual avançado, novos connectors em massa e redesign visual amplo.

## Estado inicial do repositório

- Branch: `feat/runtime-foundation`
- HEAD inicial: `228dca55c3017754c36f9967513ca27536ba798d`
- Working tree já estava sujo: `sim`
- Alterações preexistentes relevantes: preservar todos os arquivos de tarefas concorrentes, especialmente WhatsApp, ColorGlass, Company Workspace, Android e Humanizer.

## PRONTO

- [x] auditoria inicial dos módulos de persistência e consumidores principais registrada
- [x] estado inicial do Git e tarefas concorrentes reconciliado
- [x] Bloco A — schema Foundation e CRUD organizacional implementados e reconciliados
- [x] Bloco A validado com smoke SQLite temporário (`BLOCK_A_SMOKE_OK`) e `py_compile`

## EM PROGRESSO

- [ ] Bloco B — tenant isolation

## FALTA

Trabalho dividido em blocos independentes; cada bloco será validado antes do próximo:

- [ ] **B0 — Reconciliar e validar integrações parciais**: revisar `agents.py`, `connectors.py`, `workflows.py` e `tasks.py`; corrigir apenas falhas reproduzidas e rodar `py_compile` + testes focados antes de iniciar B1.
- [ ] **B1 — Concluir `tasks.py`**: proteger criação, leitura, atualização por run e propagação de `organization_id`; testar Org A vs Org B.
- [ ] **B2 — Integrar `contexts.py`**: filtrar context items, projetos e bindings por organização; validar escopos associados; testar isolamento.
- [ ] **B3 — Integrar `channels.py`**: filtrar canais e mensagens por organização/escopo; validar CRUD e contexto de agente; testar isolamento.
- [ ] **B4 — Integrar `automations.py`**: filtrar e proteger automações por organização; preservar automações legadas sem contexto; testar scheduler.
- [ ] **B5 — Contexto HTTP em `web.py`**: resolver organização por `X-Actis-Organization` e query `organization_id`; envolver requests com `organization_context`; testar endpoints.
- [ ] **B6 — Testes focados**: criar `tests/test_tenant_isolation.py` com fixtures de Org A/B, contexto legacado, cross-tenant e APIs web.
- [ ] **B7 — Documentação e validação final**: atualizar handoff/PHASE-1, executar py_compile, ruff, testes aplicáveis e registrar resultado.


## Arquivos tocados

### Modificados
- `src/meuharness/storage.py` — registrar o schema Foundation no inicializador canônico

### Criados
- `src/meuharness/enterprise.py` — entidades, validações e CRUD organizacionais
- `tests/test_enterprise_foundation.py` — testes do schema e das relações organizacionais

### Removidos
- nenhum

### Parciais / não finalizados
- nenhum ainda

## Decisões técnicas

- Usar SQLite como fonte canônica existente; o schema Foundation será criado por `CREATE TABLE IF NOT EXISTS` e registrado em `storage.init_db()`.
- Entidades organizacionais recebem `organization_id`; usuários são identidade global e entram em uma organização por `memberships`.
- `enterprise_workspaces` é o workspace empresarial; a coleção legada `company_work_items` permanece intacta para compatibilidade.
- Roles são escopadas por organização; permissions formam catálogo global e `role_permissions` aceita `scope`/condições para evolução futura.

## Testes executados

- [x] smoke SQLite temporário cobrindo schema idempotente, hierarchy CRUD, memberships/roles/permissions e rejeição de relações cross-organization — `BLOCK_A_SMOKE_OK`.
- [x] `python3 -m py_compile src/meuharness/enterprise.py src/meuharness/storage.py tests/test_enterprise_foundation.py` — aprovado.
- [ ] `pytest -q tests/test_enterprise_foundation.py tests/test_company_workspace.py tests/test_hardening.py` — não executado: `pytest` indisponível no ambiente atual.
- [ ] `ruff check ...` — não executado: `ruff` indisponível no ambiente atual.

## Estado do runtime

- Seguro continuar com runtime atual: `sim`
- Serviços reiniciados: nenhum
- Observações: não interromper serviços compartilhados; a implementação deve preservar o working tree concorrente.

## Riscos / bloqueios

- Working tree com múltiplas tarefas ativas e mudanças não commitadas.
- Storage atual usa collections JSON em SQLite; a migration precisa ser compatível e reversível.
- API atual é local e sem autenticação; será necessário introduzir contexto de tenant sem quebrar fluxos legados.

## Rollback / recovery

- Não apagar nem sobrescrever mudanças alheias.
- Manter migration idempotente e permitir acesso aos recursos antigos via organização default.
- Se a integração quebrar compatibilidade, restaurar somente os arquivos desta tarefa a partir do diff/backup local antes da alteração.

## Próximo passo exato

Editar src/meuharness/automations.py para filtrar e proteger automações por organization_id, depois adicionar contexto HTTP em src/meuharness/web.py e validar com py_compile + smoke cross-tenant.

## Próximo passo exato

Executar o **B0**: revisar o diff atual de `agents.py`, `connectors.py`, `workflows.py` e `tasks.py`, corrigir apenas falhas reproduzidas e rodar `py_compile` + testes focados antes de iniciar B1.


## Commits

- ainda não commitado

## Checkpoints de continuidade

- 2026-09-18 — tarefa criada após leitura de AGENTS.md, CURRENT-STATE, STATUS, tarefas ativas, AGENT-COMMS, git status e estrutura principal; próxima etapa é fechar o plano de migração compatível. - 2026-09-18T04:31:55-03:00 — Bloco A reconciliado e validado com smoke SQLite temporário (BLOCK_A_SMOKE_OK) e py_compile; pytest/ruff indisponíveis no ambiente. Próximo: B0, revisar tenant.py e integrações parciais antes de B1.
- 2026-09-18T05:13:18-03:00 — Retomada para concluir o Bloco B: canais já possuem coluna organization_id e validações parciais; próximo passo é integrar automations.py, contexto HTTP em web.py e testes focados; manter runtime sem restart.
